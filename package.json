{
  "private": true,
  "scripts": {
    "build": "npm run clean && gulp build",
    "clean": "gulp clean",
    "watch": "npm run clean && gulp watch",
    "test": "mocha --compilers js:babel-core/register packages/*/test/*.js"
  },
  "devDependencies": {
    "babel-core": "^6.9.1",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-es2015-node5": "^1.2.0",
    "expect": "^1.20.1",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-plumber": "^1.1.0",
    "gulp-watch": "^4.3.6",
    "lerna": "^1.1.2",
    "mocha": "^2.5.3",
    "rimraf": "^2.5.2",
    "through2": "^2.0.1"
  }
}
